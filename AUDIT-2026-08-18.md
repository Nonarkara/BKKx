# BKKx branch audit — 2026-08-18

Auditor: Claude (this session). Method: read source, run every CI gate,
re-run the generators, recompute the published numbers from the data file,
render the pages locally.
Branch state: `main`, `shophouses` and `claude/audit-main-branches-f0rkih`
**all three at `302ded5`** — zero divergence, verified by tree diff, not just
by SHA.

```
302ded5 design: density pass — the argument on screen one, the register a tool
4130edf feat: put rowhouse explorers before the research
...  (16 commits)
1552b9f feat: Global Research tab — UNESCO sites, Chinese origin ports, ...
8803ce4 feat: the citywide pressure atlas, and a Bible that shows its evidence  ← prior audit stopped here
```

**16 commits have landed since the 2026-08-16 audit and none of them had been
audited.** That, not branch divergence, is the actual subject of this pass.

Gates: **build ✓ · `tsc --noEmit` ✓ · lint ✓ · tests 41/41 ✓** (was 28/28).

---

## 0. The three branches are one tree

| Ref | SHA | Tree differs from `main`? |
| --- | --- | --- |
| `main` | `302ded5` | — |
| `shophouses` | `302ded5` | no (`git diff main FETCH_HEAD` empty) |
| `claude/audit-main-branches-f0rkih` | `302ded5` | no |

There is nothing to reconcile between branches. `shophouses` has stopped being
a feature branch and is now a second name for `main`; it has been identical at
every one of the three audits. If it is not serving a purpose, deleting it
removes a standing "which one is real?" question. If it *is* meant to be a
staging branch, it is not currently being used as one.

---

## 1. The Health Index contradicts itself on screen — **the one real defect**

`/shophouses/global` renders this, server-side (confirmed in the built worker
output, 200, 278,077 bytes):

> **Where Singapore is.** Composite **1.4** — **the floor of the index on
> purpose.** The HDB-style shophouse revival with a lifestyle economy on top
> of it is what the Bangkok Sukhumvit 71 corridor should not end up as.

`<RankingBoard />` renders immediately below that paragraph. Recomputing the
composite for all 19 towns straight from `shophouse-global.ts`
(`compositeScore` = simple mean of the five 1–5 metrics):

| # | Town | Composite | a c v r w | verdict |
| ---: | --- | ---: | --- | --- |
| 1 | hongjiang | 4.8 | 5 5 4 5 5 | ideal |
| 2 | vigan | 4.6 | 5 5 4 4 5 | vulnerable |
| 3 | kuching | 4.0 | 4 4 4 4 4 | thriving |
| … | … | … | … | … |
| 9 | **bangkok-sukhumvit-71** | 3.0 | 3 3 4 2 3 | stable |
| … | … | … | … | … |
| 16 | lijiang | **1.4** | 2 1 2 1 1 | lost |
| 17 | **singapore-chinatown** | **1.4** | 2 1 2 1 1 | lost |
| 18 | zhouzhuang | **1.4** | 2 1 2 1 1 | lost |
| 19 | **shanghai-shikumen** | **1.2** | 2 1 1 1 1 | lost |

Singapore is **not** the floor. Shanghai shikumen sits strictly below it at
1.2, and Lijiang and Zhouzhuang are tied with it at 1.4. The page asserts a
superlative that the table directly beneath it disproves — a reader scrolling
one screen sees the contradiction.

This matters more than a normal copy error because the whole page is built on
the claim that its numbers are computed rather than asserted. The number
(`1.4`) is computed correctly; the **word** "floor" around it is hand-written
and was never re-checked after the other towns were scored.

Three honest fixes, cheapest first:

1. Re-word to what is true and still makes the argument: *"Composite 1.4 —
   the bottom of the index, tied with Lijiang and Zhouzhuang."*
2. Say what was probably meant: Singapore is the lowest-scoring town that is
   still a **working commercial district** (Shanghai shikumen is largely
   residential/redeveloped). If that is the intended claim, state that
   qualifier — it is a stronger point than the current one.
3. Derive the word instead of typing it: compute `RANKED_TOWNS.at(-1)` and
   render "the floor" only when Singapore actually is it.

Option 3 is the one consistent with the rest of the file's discipline.

---

## 2. Melaka and George Town publish identical UNESCO areas

Both towns render the same figures on their own cards
(`global/page.tsx:360` prints `{t.unesco.areaHa} ha + {t.unesco.bufferHa} ha buffer`):

| Town | whcId | inscribed | criteria | areaHa | bufferHa |
| --- | ---: | ---: | --- | ---: | ---: |
| melaka | 1223 | 2008 | ii, iii, iv | **109.68** | **376.27** |
| george-town | 1223 | 2008 | ii, iii, iv | **109.68** | **376.27** |

They are one joint inscription (WHC 1223) but two physically different
component towns of different sizes. The same pair of numbers cannot be each
town's own core and buffer zone. Either these are the **whole-property
totals** — in which case they are correct but mislabelled, and the card should
say "joint property, both components" — or they are one component's figures
copied onto the other, in which case one card is wrong.

The other six UNESCO blocks (vigan 502, hoi-an 948, pingyao 812, lijiang 811,
galle 451, luang-prabang 479) each carry distinct figures, so this looks like
a copy-paste at the time the two Malaysian entries were written.

Reference numbers, inscription years and criteria all look right across all
eight. **I could not verify the areas against UNESCO — `whc.unesco.org` is
blocked by this environment's egress proxy** (see §6). The finding above is
from internal impossibility, not from checking the source.

---

## 3. Telemetry is per-event, but the rule says aggregate

`site/CLAUDE.md` states: *"D1 stores aggregate pageviews only; never collect
IP addresses or credentials."*

The explicit prohibitions are honoured — no `cf-connecting-ip`, no
`x-forwarded-for`, no credential capture anywhere in `site/worker/` or
`site/db/`. But the table is not aggregate:

```
pageviews(id, path, referrer, country, language, user_agent, created_at)
```

`recordPageview` writes **one row per request** with the user-agent string
(truncated to 512 chars), referrer, country, language and a millisecond
timestamp. That tuple is a reasonable browser fingerprint at event level, and
"aggregate pageviews only" is not what the schema does.

**This is pre-existing, not a regression** — `user_agent` dates to `38c6329`
(2026-08-10) and `site/db/` and `site/worker/` are untouched in this audit
window. Both prior audits missed it. Options: drop `user_agent` (nothing in
`pageviewStats` reads it), or amend the rule in `CLAUDE.md` to describe what
is actually collected. Either is fine; the two disagreeing is not.

---

## 4. Four high-severity CVEs in the dependency tree

```
sharp  — inherited libvips vulnerabilities
         CVE-2026-33327, CVE-2026-33328, CVE-2026-35590, CVE-2026-35591
         4 high severity
fix available via `npm audit fix --force` → next@16.3.1 (outside stated range)
```

`sharp` arrives transitively through `next` and is a build-time image
dependency; the deployed artifact is a Cloudflare Worker, so this is not
believed to be reachable at runtime. It is still a red `npm audit` on a repo
whose CI runs `npm ci` on every push. Worth a deliberate decision — pin and
document why it is not exploitable here, or take the `next` minor bump — rather
than leaving it to be rediscovered.

---

## 5. Smaller things, all in `shophouse-global.ts`

| # | Finding | Where |
| --- | --- | --- |
| 5.1 | Vigan is scored `verdict: "vulnerable"` with **continuity 5/5**, but the type comment reserves "vulnerable" for *"high-scoring towns whose continuity is at risk"* | `:91-93`, vigan block |
| 5.2 | `jakarta-kota` and `kuala-lumpur` both composite **2.4**, but one is `"lost"` and the other `"stable"` — defensible as an editor's call, but nothing records why | towns array |
| 5.3 | Section header `// ---------- National register (5) ----------` sits above **6** towns | `:552` |
| 5.4 | `RANKED_TOWNS` comment says *"Sorted ascending by composite score"*; the comparator is `compositeScore(b) - compositeScore(a)` — descending. The described outcome ("best at the top") is right, the word is wrong | `:1362-1369` |
| 5.5 | The `photo` field's doc comment is duplicated verbatim, twice in a row | `:113-116` |
| 5.6 | `protection: "precinct"` is a declared variant no town uses | `:50` |

None of these change a published number except 5.1/5.2, which are editorial
judgements the file is entitled to make — they just are not currently
explained, and the file explains everything else.

---

## 6. What held up — verified, not assumed

**The prior audit's main finding is properly fixed.** `GLOBAL_COUNTS` is
*computed* from the arrays (`TOWNS.filter(...).length`, `new Set(...).size`),
not asserted. This is the same discipline that replaced the three unsourced
constants flagged on 2026-08-16, now applied to the new page from the start.

| Check | Result |
| --- | --- |
| `npm run build` | ✓ exit 0 |
| `npx tsc --noEmit` | ✓ exit 0 |
| `npm run lint` | ✓ exit 0, clean |
| `node --test tests/rendered-html.test.mjs` | ✓ **41/41 pass**, 3.9 s |
| `build-hero-monuments.py` re-run | ✓ **byte-identical** output (md5 `49f6183…`), 67 parts |
| Working tree after a full build | ✓ **clean** — every committed generated file is in sync with its generator |
| "372 lines of dead CSS" (`3203f09`) | ✓ **exactly true** — `0` added, `372` deleted in `globals.css` |
| Edge caching (`3203f09`) | ✓ real: `max-age=0` → `max-age=60, s-maxage=60, stale-while-revalidate=300`, with the 8–10 s cold-start reason written inline |
| Font rule (no Inter/Roboto/Poppins/…) | ✓ **clean** — every "Inter" hit is a substring (`Intermediate`, `Intersection`, `esModuleInterop`), zero font declarations |
| `#c9ff38` confined to dark Console surfaces | ✓ token + `.key-walk` (selected-walk legend) used only in `AtlasView.tsx`; `zoning-planning.ts` is imported only by `AtlasView` |
| Homepage iframe stays relative | ✓ `HERITAGE_MAP_BASE = "/atlas/historic-core?embed=1"` — the `atlas.nonarkara.org` hits are prose and outbound links, never the `src` |
| Sourcing coverage | ✓ all **19** towns and all **5** origin ports carry `sources[]`; all **8** UNESCO-protected towns carry a WHC block |
| `verify-global-photos.py` exit contract | ✓ honours it — returns **1** on failure (docstring is accurate) |

The structure, for the record: **5 origin ports, 19 towns, 9 migration
routes**; protection splits 8 UNESCO / 9 national / 1 regional / 1 none.

---

## 7. What I could not verify from this checkout

This environment's egress proxy blocks more than the previous audits' did, so
several of their standard checks were impossible. **None of these are
failures — they are unknowns**, and I would not treat any as evidence of a
problem:

- **The live sites.** `shophouses.nonarkara.org` and `bkk.nonarkara.org` both
  return curl code `000` (blocked). Every earlier audit's "Live state" table
  came from fetching these. I substituted **local rendering of the built
  worker**, which confirms what the code produces but not what is deployed.
- **UNESCO figures.** `whc.unesco.org` blocked — §2 rests on internal
  contradiction only.
- **The 18 Wikimedia photos.** `verify-global-photos.py` reports "18 of 18
  failed", but every failure is `Tunnel connection failed: 403 Forbidden`
  from the proxy, not a bad URL. **This is an environment artifact — do not
  read it as 18 broken photos.** Re-run it somewhere with open egress before
  shipping a photo change; the script itself is sound.
- **`atlas.nonarkara.org`** — separate repo (`Nonarkara/bkk-3d-atlas`), not in
  this session's scope. Same caveat as both prior audits.

---

## 8. If you fix one thing

**§1.** It is the only finding that is visibly wrong to a reader, it is on the
newest and most prominent page, and it undercuts the page's own argument that
its numbers are computed rather than claimed. It is a one-line fix, and
option 3 makes it impossible to regress.
