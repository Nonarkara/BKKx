.DS_Store
*.log
.staging/
/.wrangler/
releases/*.zip
worlds/*/arnis_world_map.png
worlds/*/data/
worlds/*/icon.png
worlds/*/level.dat
worlds/*/region/
.claude/
edge-proxy/.wrangler/

# Heritage register source pulls (8 MB CSV + Overpass dumps); rebuilt by
# scripts/build-heritage-register.py --refresh
.cache/

# Original full-resolution photos Dr Non supplied for the About essay.
# Resized, renamed, web-ready copies are committed at site/public/about/ —
# these originals stay local as the source, not duplicated into git history.
public/photos/
